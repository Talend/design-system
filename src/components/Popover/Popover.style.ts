import styled from 'styled-components';
import {
	Popover as ReakitPopover,
	PopoverDisclosure as ReakitPopoverDisclosure,
	PopoverArrow as ReakitPopoverArrow,
} from 'reakit';

export const PopoverDisclosure = styled(ReakitPopoverDisclosure)``;
export const PopoverArrow = styled(ReakitPopoverArrow)``;
export const Popover = styled(ReakitPopover)``;
