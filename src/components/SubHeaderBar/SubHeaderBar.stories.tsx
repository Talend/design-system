import SubHeaderBar from '.';

export default {
	parameters: {
		component: SubHeaderBar,
	},
};

export const defaultProps = {
	title: 'Dataset name',
};

export const Default = {
	args: defaultProps,
};
