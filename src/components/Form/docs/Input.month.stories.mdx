import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { WithSelector } from '../../../docs';
import Form from '../';

<Meta title="Components/Form/Fields/Month" />

# Month

It creates input fields that let the user enter a month and year allowing a month and year to be easily entered. The value is a string whose value is in the format "YYYY-MM", where YYYY is the four-digit year and MM is the month number.

## Zoning

## Style

## States

### Default

<Canvas>
	<Story name="default">
		<Form.Month label="Month" min="2018-03" />
	</Story>
</Canvas>

### Hover

<Canvas withSource="none">
	<Story
		name="hover"
		decorators={[storyFn => <WithSelector selector=":hover">{storyFn()}</WithSelector>]}
	>
		<Form.Month label="Month" />
	</Story>
</Canvas>

### Focus

<Canvas withSource="none">
	<Story
		name="focus"
		decorators={[storyFn => <WithSelector selector=":focus">{storyFn()}</WithSelector>]}
	>
		<Form.Month label="Month" />
	</Story>
</Canvas>

### Filled

<Canvas>
	<Story name="filled">
		<Form.Month label="Month" value="2018-05" />
	</Story>
</Canvas>

### Disabled

<Canvas>
	<Story name="disabled">
		<Form.Month label="Month" value="2018-05" disabled />
	</Story>
</Canvas>

### Read only

<Canvas>
	<Story name="read-only">
		<Form.Month label="Month" value="2018-05" readOnly />
	</Story>
</Canvas>

## Interactions

## Usage

N/C

## Accessibility

N/C
