import { ArgsTable, Meta, Canvas, Story } from '@storybook/addon-docs';
import { FigmaImage, FigmaIframe, FigmaLink, Use } from '../../../docs';

import Drawer from '../';

import * as Stories from '../Drawer.stories';

import tokens from '../../../tokens';

<Meta
	title="Components/Drawer"
	component={Drawer}
	decorators={[
		storyFn => (
			<div
				style={{
					display: 'flex',
					height: '40vh',
					border: `1px solid ${tokens.colors.gray[400]}`,
					overflow: 'hidden',
				}}
			>
				<div style={{ flexShrink: 0, flexBasis: '100%' }}></div>
				<div style={{ position: 'relative', flexShrink: 0, flexBasis: '40%' }}>{storyFn()}</div>
			</div>
		),
	]}
/>

# Drawer

Drawer slides into the screen from the right side and is placed visually over the content of the page.

It aims to display extra information while keeping the navigation enabled on the main content.

## Zoning

<FigmaImage
	src="https://www.figma.com/file/qc8oUSyVJM67gSsbTShftt/%F0%9F%93%90-Zoning?node-id=744%3A263"
	alt="zoning image for drawers"
/>

## Style

<FigmaIframe
	light="https://www.figma.com/file/x0RxG7E0CzVWDycXtNgq7H/Drawer?node-id=1%3A83"
	dark="https://www.figma.com/file/x0RxG7E0CzVWDycXtNgq7H/Drawer?node-id=1%3A105"
/>

### Variations

<Story story={Stories.Simple} />

## States

Drawers are either visible or hidden.

## Content

Drawers are “optional” content living on top of primary content. They represent 1/3 of the content, prefer extra or additional information. Their content should be efficient, short and tailored for the limited layout space available.

If you need more space, use [full-page layout]().

Footer can contain

## Interaction

Close button is mandatory to hide the current Drawer.

Heading and footer are fixed the content can display a scrollbar when content overflows.

## Accessibility

Drawers disclosures have a value specified for `aria-controls` that refers to Drawer.

When Drawer is visible, disclosure has `aria-expanded` set to `true`. When Drawer is hidden, it is set to `false`.

## Usage

<Canvas>
	<Story
		name="usage"
		args={{
			visible: true,
			title: 'Title',
			children: 'Content',
		}}
		argTypes={{
			visible: {
				name: 'Drawer is visible or not',
				control: {
					type: 'boolean',
				},
			},
			title: {
				name: 'Drawer title',
			},
			children: {
				name: 'Drawer content',
			},
		}}
		parameters={{
			docs: {
				source: {
					type: 'dynamic',
					excludeDecorators: true,
				},
			},
		}}
	>
		{args => <Drawer {...args} />}
	</Story>
</Canvas>

<ArgsTable story="usage" />
